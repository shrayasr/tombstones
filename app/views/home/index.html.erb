<h1>Silence</h1>

<ul>
    <% @people.each do |person| %>
        <li>
            <h3><%= Date::MONTHNAMES[person.date_of_passing.month] %> <%= person.date_of_passing.year %></h3>
            <h2>
                <%= link_to person.name, person %>
        </li>
    <% end %>
</ul>
